import 'package:flutter/material.dart';

class ColorNavigation extends StatelessWidget {
  const ColorNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return Navigator(
      pages: [],
    );
  }
}
